{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "client": "cd client && npm start",
    "server": "nodemon --max-old-space-size=384 server.js",
    "start": "node server.js",
    "build": "npm install && cd client && npm install && npm run build",
    "reinstall": "npm ci && cd client && npm ci",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "homepage": "https://ytw-stream.herokuapp.com/",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "agentkeepalive": "^4.2.1",
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^2.0.20"
  }
}
