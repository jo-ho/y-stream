{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "client": "cd client && npm start",
    "server": "node --max-old-space-size=256 server.js",
    "start": "node server.js",
    "build": "npm install && cd client && npm install && npm run build",
    "reinstall": "npm ci && cd client && npm ci",
    "dev": "node --max-old-space-size=256 server.js && npm run client"
  },
  "homepage": "https://ytw-stream.herokuapp.com/",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "agentkeepalive": "^4.2.1",
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^2.0.20"
  }
}
